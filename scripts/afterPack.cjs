const fs = require('node:fs');
const path = require('node:path');
module.exports = async (context) => {
  // Ensure userData/settings exists at pack-time on first run: nothing to do here, runtime handles it.
};
